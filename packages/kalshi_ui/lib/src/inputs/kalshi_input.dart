import 'package:flutter/widgets.dart';

class KalshiInput extends StatelessWidget {
  const KalshiInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
